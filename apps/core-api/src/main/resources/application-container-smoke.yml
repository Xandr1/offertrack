management:
  endpoint:
    health:
      show-components: always
      show-details: never
      group:
        readiness:
          include: readinessState,db,redis

app:
  ai-service:
    auth-mode: internal-key
    audience:
